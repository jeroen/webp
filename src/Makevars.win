PKG_CPPFLAGS= -I../windows/webp-0.6.0/include
PKG_LIBS= -L../windows/webp-0.6.0/lib${R_ARCH} -lwebp

all: clean winlibs

winlibs:
	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" "../tools/winlibs.R"

clean:
	rm -f $(SHLIB) $(OBJECTS)
